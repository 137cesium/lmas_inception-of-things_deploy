apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wil42-playground
  namespace: dev
spec:
  entryPoints:
    - app8888
  routes:
    - match: PathPrefix(`/test`)
      kind: Rule
      services:
        - name: wil42-playground
          port: 80    